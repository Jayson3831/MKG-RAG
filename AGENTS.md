# 项目约定

- 默认使用极简中文，结论先行。
- 根目录仅保留项目入口、约定和必要工程配置；不要将中间文档、日志、图表或数据放在根目录。
- `docs/research/research-ideas.md` 为持续迭代的《研究思路》，维护现阶段的研究方案，并在整体可行性得到验证后形成完整技术方案。
- 验证路线、候选实验及共用证据口径维护于 `docs/experiments/validation-plan.md`。
- 每个实验的任务书单独写在 `docs/tasks/<实验编号>.md`，包含执行方案、交付要求和审核指南；E001 使用 `docs/tasks/E001.md`。
- **`docs/experiments/validation-log.md` 只保存验证完成后的可行或不可行分析结果**，包括结论、关键证据、原因分析及适用范围；证据不足时明确无法判定，不强行归为不可行。不得放入任务书、待执行计划、方法说明、审核教程、空白模板或过程流水账。
- 可复用代码放 `src/`，执行入口放 `scripts/`，配置放 `configs/`，必要测试放 `tests/`。
- 原始数据放 `data/raw/` 并保持不变；派生数据放 `data/processed/`，记录来源与处理配置。
- 每次实验创建 `outputs/runs/<YYYYMMDD-HHMMSS>_<experiment-id>/`，保存配置快照、输入清单和校验值、环境/代码版本、命令、日志、指标及证据。记录时间时包含时区；同名运行目录不可覆盖。
- 运行状态、失败日志、实际命令、配置偏离、待审核信息及逐条审计保存在对应运行目录。完成审核后的分析按实验编号、运行编号追加到实验验证记录，保留负结果及旧结论的适用范围，不静默覆盖历史结果。
- 严格区分计划、实测、推断和待核验事项；未运行时标注“未执行”，不得填写虚构指标。
- 未经用户要求，不为整理文件引入模型训练、全量下载或其他大规模实验。

## Git 分支、每日推送与合并

- 完整执行规则见 `docs/git-workflow.md`。E001 在 `exp/e001` 独立分支持续开发、修复和每日推送，直到版本确认；日常开发不得直接在 main 上进行。
- 服务器 coding agent 负责提交及推送，本地审核 agent 通过 Git 获取目标版本。代码与文档统一用 Git，同步前检查分支、远程目标及未提交修改，不用 SFTP 覆盖代码。
- 每个可说明的修改及时提交；每天下班前及当次任务结束/暂停前检查并推送实验分支。未完成工作可用 WIP 提交备份，如实记录测试状态；不创建无意义空提交。
- 推送成功须核对远程分支 tip 与本地 HEAD 的完整 SHA 一致。失败保留提交并报告；不强制推送、不改写已发布历史、不覆盖其他工作。
- 提交前检查暂存文件及差异，仅提交代码、配置、测试、依赖和文档；遵守 .gitignore，不提交数据、运行产物、环境或凭据，不用 git add -f 绕过规则。
- 正式实验绑定干净提交与配置哈希，使用固定提交的工作树/快照执行；manifest 保存实际 code_commit，不以之后的分支 tip 替换。
- 审核修复在实验分支完成。只有用户明确确认最终待合并 SHA 且相关检查通过后才经 PR 合并 main；每日推送和任务完成不构成合并授权。
- 合并默认保留提交历史；不自动删除分支、发布或部署。后续独立实验从届时稳定 main 创建新实验分支。
- 结束检查点不代表已配置定时任务；无人运行的 agent 不保证定点推送。仓库地址、权限和主分支基线未配置前不得猜测目标或声称已推送。

## 远程执行与 SFTP 交付

- SFTP 使用 `.vscode/sftp.json` 的 `bootstrap-upload` / `download-review` 两个 profile；操作及完整排除规则见 `docs/sftp-sync.md`。
- 远程下载的大数据及索引放 `data/`，运行专属的大型中间产物放 `outputs/runs/<运行编号>/server-only/`，这些内容不回传本地。不要把大型数据或其符号链接放入代码、文档或审核目录。
- 代码和配置通过 Git 同步；SFTP 回传完整候选账本、查询结果、证据证书、审核表及必要日志。全量 `normalization.jsonl` 留在服务器，提供小型标准化样本及完整映射的来源引用；禁止为缩小体积删除失败候选或必要证据。
- 交付前生成 `download-manifest.json`，列出回传文件的大小、SHA-256、总大小及仅服务器保存的文件位置。建议回传总量不超过 200 MB、单文件不超过 50 MB；超出时报告，不静默截断。SFTP 只按路径和后缀过滤，无法自动保证体积上限。
- 审核结果使用未压缩 JSON/JSONL/HTML/TXT 等可同步形式；大型归档、数据库、模型和缓存不回传。没有全量本地数据时，将依赖完整图的复核标记为需要远程执行，不声称已本地重放。

## 研究思路的迭代规则

- 每次讨论或实验产生有效结论后，更新《研究思路》：将当前采用且有依据的可行思路纳入主章节，将已判定不可行或已淘汰的内容从主章节移除，保持全文一致。
- 主章节只保留现阶段的最终方案，直接说明采用什么、如何实现及适用条件。不要在主章节叙述推翻过程或使用“因为原先 A 不可行，所以改用 B”等表达，直接写当前提出的 B。
- “现阶段最终方案”指当前采用的工作方案，不等于已被实验证实。保留必要的待验证假设和证据边界；尚无证据的候选明确标记为待验证，不伪装为已可行。
- 淘汰与替代过程集中记录在《研究思路》的独立章节“思路淘汰与替代记录”：写明旧思路、被推翻的假设、为什么被推翻、证据及适用范围、新思路和新思路的验证状态。
- 区分方法设计审查、实验否证和工程失败。方法审查不能写成实验推翻；单次运行失败只有在支持相应结论时，才能用于判定研究思路不可行。
- 《实验验证计划》保存验证设计，独立任务书负责执行交接，《实验验证记录》仅汇总验证完成后的分析结论；《研究思路》呈现据此形成的当前技术方案，独立淘汰章节只保留原因摘要与证据链接，不复制完整实验日志。
- 淘汰思路时，同步更新受影响的目标、数据选择、模块、流程、指标及下一步安排；历史实验记录继续保留，避免当前方案与旧结论混杂或互相矛盾。
- 当关键假设及整体流程的可行性得到验证时，将同一文档整理为完整技术方案，覆盖问题定义、数据与接口、算法流程、关键模块、证据与对齐机制、成本控制、评测结论、适用边界和实施安排；不另建并行方案副本。

## 会话交接与职责

- 会话恢复入口为 `docs/handoff.md`；交接快照不代替实际文件、Git 版本和运行证据。
- 本地 Codex 负责研究方案、任务书和代码/证据审核；远程 coding agent 负责实现、运行、修复、Git 提交推送及交付；用户负责实际人工审计与最终版本确认。
- 交付区分任务规范 `spec_commit`、实际运行 `code_commit` 和最终候选 `candidate_commit`，均使用真实完整 SHA；AI 技术审核不得代填人工审计状态。
